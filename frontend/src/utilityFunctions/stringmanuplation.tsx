// import React from 'react';

export const capitalizeFirstLetterOfWord = ({ textString }: { textString: string }) => {
    const str2 = textString.charAt(0).toUpperCase() + textString.slice(1);
    console.log(str2);


  return (
      str2
  )
}
